/**
 * GetLocalDataset handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: local-datasets
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getLocalDataset } from "@splitward/api-server/domains/local-datasets/handlers";
import type { LocalDatasetsDDDDependencies } from "@splitward/api-server/domains/local-datasets";
import { createLocalDataset as createLocalDatasetEntity } from "@splitward/factories/local-datasets";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@splitward/tests/shared";
import { LOCALDATASETS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetLocalDataset", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LocalDatasetsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LocalDatasetsDDDDependencies>(
      LOCALDATASETS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createLocalDatasetEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetLocalDataset, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { datasetId: entity.id };

    await getLocalDataset(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetLocalDataset.execute).toHaveBeenCalledWith(expect.objectContaining({ datasetId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { datasetId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetLocalDataset, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getLocalDataset(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
