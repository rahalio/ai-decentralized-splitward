/**
 * ListLocalDatasets handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: local-datasets
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listLocalDatasets } from "@splitward/api-server/domains/local-datasets/handlers";
import type { LocalDatasetsDDDDependencies } from "@splitward/api-server/domains/local-datasets";
import { createLocalDataset as createLocalDatasetEntity } from "@splitward/factories/local-datasets";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@splitward/tests/shared";
import { LOCALDATASETS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListLocalDatasets", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LocalDatasetsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LocalDatasetsDDDDependencies>(
      LOCALDATASETS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createLocalDatasetEntity({ orgId }),
      createLocalDatasetEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListLocalDatasets, "execute").mockResolvedValue(useCaseResult);

    await listLocalDatasets(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListLocalDatasets.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
