/**
 * Site Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: sites
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { SiteEntity } from "@splitward/core/sites/models/index.js";
import { sitesSchemas } from "@splitward/core/sites";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "sit";

export function createSite(overrides: Partial<SiteEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    maxEgressMbps: overrides.maxEgressMbps ?? faker.number.int(),
    maxGpu: overrides.maxGpu ?? faker.number.int(),
    name: overrides.name ?? faker.person.fullName(),
    siteId: overrides.siteId ?? `sit_${ulid().toLowerCase()}`,
    status: overrides.status ?? "active",
    ...overrides,
  };

  return sitesSchemas.Site.parse(raw);
}

export function siteBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createSite(overrides));
}
