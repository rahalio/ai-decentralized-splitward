/**
 * ListTrainingRounds handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: training-rounds
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listTrainingRounds } from "@splitward/api-server/domains/training-rounds/handlers";
import type { TrainingRoundsDDDDependencies } from "@splitward/api-server/domains/training-rounds";
import { createTrainingRound as createTrainingRoundEntity } from "@splitward/factories/training-rounds";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@splitward/tests/shared";
import { TRAININGROUNDS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListTrainingRounds", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TrainingRoundsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TrainingRoundsDDDDependencies>(
      TRAININGROUNDS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createTrainingRoundEntity({ orgId }),
      createTrainingRoundEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListTrainingRounds, "execute").mockResolvedValue(useCaseResult);

    await listTrainingRounds(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListTrainingRounds.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
