/**
 * LocalDatasets DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: local-datasets
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * LocalDatasets Domain Module - Composition root
 *
 * DDD: Composition root for local-datasets domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  LocalDatasetRepositoryAdapter,
} from "@splitward/adapters/local-datasets";
import { getIdGeneratorService } from "@splitward/adapters";
import type { AdapterDynamoDBClient } from "@splitward/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetLocalDataset,
  ExecuteListLocalDatasets,
  ExecuteRegisterLocalDataset,
} from "@splitward/services/local-datasets/usecases";
// Ports (interfaces)
import type {
  LocalDatasetRepository,
} from "@splitward/services/local-datasets/ports";

export interface LocalDatasetsDomainModule {
  repos: {
    localDatasets: LocalDatasetRepository;
  };
  useCases: {
    localDatasets: {
      create: ExecuteRegisterLocalDataset;
      get: ExecuteGetLocalDataset;
      list: ExecuteListLocalDatasets;
    };
  };
}

export function buildLocalDatasetsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): LocalDatasetsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      localDatasets?: LocalDatasetRepository;
    } = {};

    obj.localDatasets = new LocalDatasetRepositoryAdapter(dynamoClient);
    return {
      localDatasets: obj.localDatasets!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    localDatasets: {
      create: new ExecuteRegisterLocalDataset(executionContext, idGenerator, repos.localDatasets),
      get: new ExecuteGetLocalDataset(executionContext, idGenerator, repos.localDatasets),
      list: new ExecuteListLocalDatasets(executionContext, idGenerator, repos.localDatasets),
    },
  };
  return { repos, useCases: useCases as LocalDatasetsDomainModule["useCases"] };
}
