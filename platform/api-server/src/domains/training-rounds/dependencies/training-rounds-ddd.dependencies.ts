/**
 * TrainingRounds DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: training-rounds
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * TrainingRounds Domain Module - Composition root
 *
 * DDD: Composition root for training-rounds domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  TrainingRoundRepositoryAdapter,
} from "@splitward/adapters/training-rounds";
import { getIdGeneratorService } from "@splitward/adapters";
import type { AdapterDynamoDBClient } from "@splitward/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetTrainingRound,
  ExecuteListTrainingRounds,
  ExecuteStartTrainingRound,
} from "@splitward/services/training-rounds/usecases";
// Ports (interfaces)
import type {
  TrainingRoundRepository,
} from "@splitward/services/training-rounds/ports";

export interface TrainingRoundsDomainModule {
  repos: {
    trainingRounds: TrainingRoundRepository;
  };
  useCases: {
    trainingRounds: {
      create: ExecuteStartTrainingRound;
      get: ExecuteGetTrainingRound;
      list: ExecuteListTrainingRounds;
    };
  };
}

export function buildTrainingRoundsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): TrainingRoundsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      trainingRounds?: TrainingRoundRepository;
    } = {};

    obj.trainingRounds = new TrainingRoundRepositoryAdapter(dynamoClient);
    return {
      trainingRounds: obj.trainingRounds!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    trainingRounds: {
      create: new ExecuteStartTrainingRound(executionContext, idGenerator, repos.trainingRounds),
      get: new ExecuteGetTrainingRound(executionContext, idGenerator, repos.trainingRounds),
      list: new ExecuteListTrainingRounds(executionContext, idGenerator, repos.trainingRounds),
    },
  };
  return { repos, useCases: useCases as TrainingRoundsDomainModule["useCases"] };
}
