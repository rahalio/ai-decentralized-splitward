/**
 * ExecuteListStudies - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: studies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListStudies Use Case
 *
 * DDD: Application use case for list Study.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListStudiesInput, ListStudiesOutput } from "../dto/study.dto";
import type { ExecutionContextService, IdGeneratorService } from "@splitward/services/_shared/index.js";
import type { StudyRepository } from "../ports";

export class ExecuteListStudies {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly study: StudyRepository
  ) {}

  async execute(input: ListStudiesInput): Promise<ListStudiesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.stuId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.study.listStudies({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListStudiesOutput;
  }
}
