/**
 * ExecuteGetLocalDataset - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: local-datasets
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetLocalDataset Use Case
 *
 * DDD: Application use case for get LocalDataset.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetLocalDatasetInput, GetLocalDatasetOutput } from "../dto/local-dataset.dto";
import type { ExecutionContextService, IdGeneratorService } from "@splitward/services/_shared/index.js";
import type { LocalDatasetRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetLocalDataset {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly localDataset: LocalDatasetRepository
  ) {}

  async execute(input: GetLocalDatasetInput): Promise<GetLocalDatasetOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ldsId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).datasetId) {
      throw new ValidationError("datasetId is required");
    }
    const datasetId = (input as any).datasetId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.localDataset.getLocalDataset({ datasetId: datasetId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${datasetId}`);
    }

    return entity as GetLocalDatasetOutput;
  }
}
